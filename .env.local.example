# Leave unset and the import runs on a built-in sample camera roll.
# Set it and /api/import reads the real folder — metadata only, never writes.
DROPBOX_ACCESS_TOKEN=
DROPBOX_CAMERA_FOLDER=/Camera Uploads
