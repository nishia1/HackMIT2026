# Leave these unset and the import runs on a built-in sample camera roll.
# Set them and /api/import reads your real folder — metadata only, never writes.
#
# From https://www.dropbox.com/developers/apps — scopes files.metadata.read
# and files.content.read. Then run `npm run dropbox:auth` for the refresh token.



DROPBOX_APP_KEY=vsitcpsby2laqox
DROPBOX_APP_SECRET=xzpngkxr33ocnrx
DROPBOX_REFRESH_TOKEN=RNHEfIzLmXoAAAAAAAAAAZyydfBQNNd1p-1IifAQdpdCusI8KttZRAPwRK0-VLjJ

# A four-hour token from the App Console, if you'd rather skip the OAuth step.
DROPBOX_ACCESS_TOKEN=

DROPBOX_CAMERA_FOLDER=/hackmit

# Confirmed events, and who was at them.
MONGODB_URI="mongodb+srv://carolynnlee950_db_user:carolyn123@cluster0.j2ouuz1.mongodb.net/?retryWrites=true&w=majority"
MONGODB_DB=invisible-string

# Added during the auth migration. Signs sessions and the Dropbox OAuth state.
AUTH_SECRET=JUZUZj/tRe8vVi13+/PMBFxoVkr+RFMSX7JUtmPjun4=

# Dropbox is the sign-in. Redirect URI to register in the App Console:
#   http://localhost:3000/api/auth/callback/dropbox
